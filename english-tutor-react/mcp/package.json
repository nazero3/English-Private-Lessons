{
  "name": "kinz-mcp-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Read-only local MCP sidecar for the Kinz Teacher Platform API. Does not deploy with the live app.",
  "bin": {
    "kinz-mcp-server": "./src/index.js"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.0",
    "zod": "^3.25.0"
  }
}
